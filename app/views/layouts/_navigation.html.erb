<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header" style="text-align: center;">
                <div class="dropdown profile-element">
                    <!-- <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">Example user</strong>
                            </span> <span class="text-muted text-xs block">Example menu <b class="caret"></b></span>
                        </span>
                    </a> -->
                    <img src="/assets/img/logo.png">
                    <!-- <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><%= link_to "Logout", user_login_path %></li>
                    </ul> -->
                </div>
                <div class="logo-element">
                    ES+
                </div>
            </li>
            <li class="<%= 'active' if is_active_controller('user')&&is_active_action('index') %>">
                <%= link_to user_index_path do %>
                <i class="fa fa-user-plus"></i> <span class="nav-label">Creación de Usuario</span>
                <% end %>
            </li>
            <li class="<%= is_active_controller('user')&&is_active_action('search') %>">
                <%= link_to user_search_path do %>
                <i class="fa fa-search"></i> <span class="nav-label" data-i18n="nav.layouts">Buscador de usuarios</span>
                <% end %>
            </li>
            <li class="<%= is_active_controller('saleshistory') %>">
                <%= link_to saleshistory_index_path do %>
                <i class="fa fa-database"></i> <span class="nav-label" data-i18n="nav.layouts">Reportes</span>
                <% end %>
            </li>
            <li class="<%= is_active_controller('user')&&is_active_action('profile') %>">
                <%= link_to user_profile_path do %>
                <i class="fa fa-user-circle"></i> <span class="nav-label" data-i18n="nav.layouts">Mis datos</span>
                <% end %>
            </li>
            
        </ul>

    </div>
</nav>